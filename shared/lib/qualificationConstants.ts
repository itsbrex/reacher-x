/**
 * Single source of truth for qualification score thresholds (0–100).
 * Used by Convex qualification logic, prompts, and client UI (e.g. onboarding fit range).
 */
export const QUALIFICATION_THRESHOLD = 70 as const;
